The package divided panels contains an alternative layout that works very differently 
from the grid layouts. 
Currently, there are few uses for it, so it is not covered in the instruction manual.
However, curious users and programmers might want to play with it.